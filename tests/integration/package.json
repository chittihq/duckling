{
  "name": "integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm run typecheck && vitest run"
  },
  "dependencies": {
    "@chittihq/duckling": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "typescript": "^5.3.3"
  }
}
